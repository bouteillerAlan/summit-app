import React, { type ReactElement } from 'react';
import { Text } from 'native-base';

const ListWorkout = (): ReactElement => {
  return (
    <Text>ListWorkout page</Text>
  );
};

export default ListWorkout;
